{{#if script}}
<script setup lang="ts">
import { useNamespace } from '../../shared/hooks'

defineOptions({ name: 'M{{ pascalCase component_name }}', inheritAttrs: false })

const { b } = useNamespace('{{ pascalCase component_name }}')
const getClass = computed(() => {
  return [b()]
})
</script>
{{/if}}

{{#if template}}
<template>
  <div :class="getClass"></div>
</template>
{{/if}}
